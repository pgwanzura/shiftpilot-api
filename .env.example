# Application Configuration
APP_NAME="ShiftPilot API"
APP_ENV=local
APP_KEY=base64:yo0P0aCjM6Ay4VfbHBNKpb0qfaYvGaR7oF/69wYnM4E=
APP_DEBUG=true
APP_URL=http://api.localhost
FRONTEND_URL=http://frontend.localhost

# Internationalization
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Maintenance
APP_MAINTENANCE_DRIVER=file

# PHP Configuration
PHP_CLI_SERVER_WORKERS=4

# Security
BCRYPT_ROUNDS=12

# Logging
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=shiftpilot
DB_USERNAME=shiftpilot
DB_PASSWORD=password

# Session Configuration
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=.localhost
SESSION_SECURE_COOKIE=false
SANCTUM_STATEFUL_DOMAINS=frontend.localhost,localhost:3000,127.0.0.1:3000,localhost,127.0.0.1

# Broadcasting
BROADCAST_DRIVER=log  # ← CHANGED FROM BROADCAST_CONNECTION
FILESYSTEM_DISK=local

# Queue Configuration
QUEUE_CONNECTION=redis

# Cache Configuration
CACHE_DRIVER=redis

# Redis Configuration (FIXED)
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=redispassword
REDIS_PORT=6379

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@shiftpilot.localhost"
MAIL_FROM_NAME="${APP_NAME}"

# Frontend URL for CORS
FRONTEND_URL=http://frontend.localhost

# API Configuration
API_PREFIX=api
API_VERSION=v1

# Application Specific
SHIFTPILOT_COMMISSION_RATE=2.0
SHIFTPILOT_PLATFORM_FEE=0.30

# Stripe Configuration (update with real test keys)
STRIPE_KEY=pk_test_your_stripe_publishable_key
STRIPE_SECRET=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret